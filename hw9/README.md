# Homework 9

- [`ddl/`](ddl/) publishes the minimum resume-schema contract and the four metadata queries used to evaluate tables, columns, primary keys, and foreign keys.
- [`rest/`](rest/) publishes the REST/JSON contract, PHPUnit suite, and deterministic local service fixture.

The previous single-test/result-file implementation is retained under [`../legacy/hw9-original/`](../legacy/hw9-original/) and is not current.
