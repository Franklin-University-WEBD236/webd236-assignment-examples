# Homework 7: Objects and functional programming

The public tests cover the `Car` class, the callback returned by `modeMaker()`, and the `reduce()` function. They exercise construction, state changes, fuel limits, callable reducers, and repeated values.
