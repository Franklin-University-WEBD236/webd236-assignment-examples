# Homework 5: String functions

The public PHPUnit files test `isPalindrome()`, `myReverse()`, `isLetter()`, `longestRun()`, and `countdownFront()`.

Place the relevant test beside the PHP file containing the required function. The tests cover ordinary inputs, boundary characters, repeated runs, and representative longer strings.
