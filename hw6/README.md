# Homework 6: Arrays and file processing

The public tests cover `removeDuplicates()`, `removeAllValuesMatching()`, and `findSpellings()`. Array keys and retained values are checked where the contract requires them. `findSpellingsTest.php` uses the included `words.txt` fixture.
